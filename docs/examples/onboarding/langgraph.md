# LangGraph onboarding

Install `atmem[langgraph]` and pass `create_langgraph_middleware(...)` to the
agent middleware list. Keep the native checkpoint store authoritative for host
state; AtMem governs memory and evidence.
