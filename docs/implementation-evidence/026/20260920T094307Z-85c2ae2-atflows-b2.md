# AtFlows default-install source-candidate evidence

**Feature/task/requirements:** Spec 026 T028; FR-016 (host-neutral local operation) and FR-018 (compatible upgrades and opt-in behavior). This entry verifies the local 2.3.4b2 package and CLI checkpoint. It does not verify the 2.4.0 Memory Health queue, remote CI, publication or causal influence.

**Source:** branch `jev-production-ui`, base commit `85c2ae20658bb020a6aba41abfcd0cb85119cfd4`, dirty working tree containing the candidate changes. The source has not been committed, tagged or published. The installed candidate wheel at `/tmp/atmem-b2-dist/atmem-2.3.4b2-py3-none-any.whl` has SHA-256 `f6bc71269204b473f575adb246f0cfdadc15d4d4422a478151adb94a1df04e93`. Temporary test paths are local artifacts, not durable CI artifacts.

**Host/configuration:** macOS Darwin 25.5.0 arm64; test Python 3.12.2 under x86_64 translation, pytest 9.1.1. The fresh installed-wheel environment `/tmp/atmem-b2-venv` contains AtMem 2.3.4b2, AtFlows 0.1b6 and AtBot 0.1.0. The AtFlows handoff used a local AtFlows 0.1b6 server and one synthetic OTLP trace; no customer data or remote model was used. Bun is needed to run the separate AtFlows server, but not for ordinary AtMem retrieval.

**Recorded verification, 2026-09-20 UTC:** At 09:43:07.219, ran `python -m pytest -q tests/test_cli_guidance.py tests/test_atflows_review.py tests/test_release_metadata.py tests/test_control_evidence.py tests/test_website_docs.py`. It ended at 09:43:11.667 with 39 passed and no skips. Immediately ran `python scripts/check_website_docs.py`; it ended at 09:43:11.737 with 46 pages and version 2.3.4b2 valid. `git diff --check` also passed. The full local `python -m pytest -q` run completed with 1,621 passed, no skips and one external Pydantic AI deprecation warning in 175.21 seconds; its exact start/end timestamps were not captured, so the recorded focused command above is the reproducible timed gate. CI/publish jobs have not run for this source candidate.

**Installed-artifact checks:** A fresh macOS virtual environment installed the AtMem wheel and automatically resolved AtFlows 0.1b6 and AtBot 0.1.0; `pip check` passed. Installing the same wheel using the legacy `[atflows]` extra also succeeded. A separate Python 3.12 environment installed published AtMem 2.3.4b1, created a persisted memory/control-plane fixture, upgraded to the candidate wheel and passed `tools/smoke_upgrade_from_2_1.py verify` with memory recall, identity, protected evidence and control schema intact. The installed-wheel `tools/smoke_installed_package.py` and `tools/smoke_atflows_handoff.py` checks passed. The latter verified exact session linkage, a read-only trace-error lead, redaction of private sample text, JSON and human output, and stable correlated/unverified report keys. These earlier local runs were recorded without exact UTC intervals and are not independently verified remote CI results.

**Operator behavior:** A local `atmem init --no-open --port 59999` displayed installed companion versions, a dashboard URL and a one-time copyable Administrator password. `atmem status` displayed installed versions and bounded numeric-loopback dashboard links without reading an existing password or invoking AtBot setup. AtFlows installation does not start its server, enable tracing, import trace bodies, change canonical memory or establish causal memory influence. The report requires an authorized AtMem evidence token and a separately configured AtFlows Administrator password.

**Evidence class and limitations:** Local installed-host smoke plus simulated synthetic trace; no independent hosted measurement, human protocol, remote workflow result, PyPI publication or npm publication. The source working tree may change after this entry; rerun installed-artifact and remote gates on the reviewed commit before tagging. There is no earlier Spec 026 entry to supersede.
