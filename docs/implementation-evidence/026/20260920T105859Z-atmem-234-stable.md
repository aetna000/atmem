# AtMem 2.3.4 stable local release gates — 2026-09-20

The source tree pins AtFlows 0.1.1 and AtBot 0.1.0. AtFlows 0.1.1 was verified on PyPI and by a fresh-index installation. These local checks do not substitute for tagged GitHub Actions or registry verification of AtMem 2.3.4.

- Python suite: 1,622 passed, one external Pydantic AI deprecation warning.
- AtFlows delegated auth: two Bun tests; AtFlows server suite; dashboard build and typecheck (three existing warnings). A live two-dashboard test verified AtMem-owned sign-in, role, protected API, disabled AtFlows local users, and shared logout.
- Installed wheels: AtMem 2.3.4, AtBot 0.1.0 and AtFlows 0.1.1 resolved in a clean venv; `pip check`, AtMem installed-wheel smoke, and the optional AtFlows standalone handoff passed. A separate installed AtFlows delegated-login smoke passed.
- OpenClaw bridge 2.3.4: `npm ci`, typecheck, tests and smoke passed.
- AtMem and AtFlows wheel builds and Twine metadata checks passed. AtFlows 0.1.1 includes the runtime/dashboard assets and required license files.
- Public docs manifest: 48 pages, version 2.3.4, inventory valid. Website preview import/build/gates/docs tests/browser checks passed against uncommitted public source; this preview is not a production publication.

## Published release verification

The separate remote gates completed for the stable release. The reviewed release commit is `121cdb786309c9eab256b08632204f312195a33d` on `release/2.3.4b2-candidate`; annotated tag `v2.3.4` points to that commit. The [tagged publish workflow](https://github.com/aetna000/atmem/actions/runs/35507676806) passed all 34 jobs. The [GitHub release](https://github.com/aetna000/atmem/releases/tag/v2.3.4) is published as a full release, and fresh registry installs verified `atmem==2.3.4`, `atmem-atbot==0.1.0`, and `atflows==0.1.1` with `pip check`. The matching npm OpenClaw bridge `openclaw-memory-atmem@2.3.4` is published under the `latest` dist-tag. The published-version upgrade matrix passed.

[AtFlows 0.1.1](https://github.com/aetna000/atflows/releases/tag/v0.1.1) is also a stable release on GitHub and PyPI. Its optional delegated login uses AtMem-owned accounts; standalone login remains available when delegation is not configured.

The website main commit `857049a99ed7f3b327309d24c01fd1101097da57` passed its validation workflow (run `35508017266`). Its validated archive passed SHA-256 verification and was deployed to Firebase Hosting only. The live `https://atmem.ai/` homepage, documentation, portfolio, and AtFlows guide returned HTTP 200, and the imported docs identify AtMem source revision `121cdb786309c9eab256b08632204f312195a33d`.

This completes the 2.3.4 checkpoint T028. The Memory Health queue remains planned for 2.4.0.
